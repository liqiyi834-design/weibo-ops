# 运营控制台

每天先打开这个文件。

## 项目化规则

2026-05-14 已吸收 GitHub 更新：本项目进入 HotComment-AI「选题推荐 + 候选池 + 风格配置」阶段。

- 项目定位：热点锐评草稿生成工具 + MCP 工具服务 + 自动化草稿任务 + 人工审核机制。
- 默认只生成可审核草稿，不自动发布、不自动评论、不批量互动、不绕过平台风控。
- 内容原则：事实要稳，观点要狠，表达要像人，边界要守住。
- 标准链路：topic/context -> 事实摘要 -> 话题分类 -> RAG 检索 -> 观点生成 -> 人格化改写 -> 安全审查。
- 工程改动优先复用 `app/services`；FastAPI 与 MCP 都调用同一套核心逻辑。
- FastAPI 当前已有：热搜拉取、评论生成、账号/风格查询、选题推荐、候选池、知识库重建与检索。
- MCP 当前已有：`get_hot_topics`、`select_comment_topics`、`generate_comment`、`rebuild_knowledge`、`search_knowledge`；本地增强中加入 `get_ent_topics` 用于文娱榜。
- 选题推荐输出必须看：`score`、`reason`、`risk_level`、`recommended_angle`、`avoid_points`。
- 候选池状态流转：`candidate` -> `researched` -> `selected` / `skipped` -> `drafted` / `published`。
- 下一批能力优先级：`save_draft`、`list_drafts`、`safety_check`、`research_topic`、`ingest_topic_research`。
- 草稿箱是自动化与人工发布之间的缓冲层，后续生成内容应优先可保存、可审查、可追溯。
- 不把 API key、cookie、token、真实 `.env` 写入文档或代码。

## 今日任务看板

| 优先级 | 任务 | 工具/入口 | 产物 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |
| P0 | 拉取当前热搜榜 | `get_hot_topics` / `/api/hot/weibo` | 实时榜单 | 待执行 | 用户要求生成博文前必须重拉 |
| P0 | 拉取文娱榜 | `get_ent_topics` | 文娱榜单 | 待执行 | 与热搜榜重合则升优先级 |
| P0 | AI 选题闸门 | `select_comment_topics` / `/api/topics/select` | 3-5 个候选题 | 待执行 | 可开 `enrich_metrics=true` 二次采样 |
| P0 | 词条采样 | 微博词条页/智搜/高赞博文 | 事实脉络 + 引爆点 | 待执行 | 没采到必须标注“缺词条样本” |
| P1 | 候选池入库 | `/api/topic-candidates/pools` | JSON 候选池 | 待执行 | 默认目录 `output/topic_candidates/` |
| P1 | 生成草稿 | `/api/comment/generate` / `generate_comment` | 可审核正文 | 待执行 | 默认账号 `today_direct` |
| P1 | 风格校准 | `/api/accounts` / `/api/comment/styles` | 风格选择 | 待执行 | 高风险题默认 `rational_critic` |
| P2 | 发布后复盘 | `16_增长指标看板.csv` | 阅读/评/转/赞记录 | 待执行 | 20 分钟低于 100 阅读，换题 |

## 今日状态

日期：

账号状态：

- 正常 / 限流观察 / 审核异常 / 暂停发布

今日目标：

- 发布数量：
- 重点栏目：
- 重点话题：
- 热度优先执行：是/否
- 今日冲榜实验：是/否

## 实时拉榜记录

| 北京时间 | 榜单来源 | 是否实时 | 榜首 | 备注 |
| --- | --- | --- | --- | --- |
|  |  | 是/否 |  | 超过 10 分钟未更新则作废 |

## 今日选题池

| 优先级 | 热搜排名 | 文娱排名/双榜 | 主题 | 标识/热度 | 选题分 | 推荐角度 | 词条样本 | 风险 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| S/A/B/C |  |  |  | 新/热/爆/沸/无 |  |  | 已采/缺样本 | 低/中/高 | candidate/researched/selected/skipped/drafted/published |

## 词条采样记录

| 热搜词 | 排名 | 标识 | 事件阶段 | 引爆博文共同点 | 评论区分歧 | 年轻人情绪 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  | 新/热/爆/沸/无 | 起势/冲高/反转/降温/二次发酵 |  |  |  |

## 今日冲榜实验

项目名：

关联热榜：

目标人群：

核心情绪：

可传播短句：

评论区问题：

风险压低方式：

## 今日发布队列

| 时间 | 标题/主题 | 来源候选池 | 类型 | 风格 | 风险 | 是否需核查 | 发布状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 09:00 |  |  | 快评 | rational_critic |  | 是/否 | 待发 |
| 11:30 |  |  | 科普 | rational_critic |  | 是/否 | 待发 |
| 13:00 |  |  | 互动 | ironic_observer |  | 是/否 | 待发 |
| 15:30 |  |  | 观点 | rational_critic |  | 是/否 | 待发 |
| 18:30 |  |  | 轻松观察 | ironic_observer |  | 是/否 | 待发 |
| 21:30 |  |  | 热点后续/夜间快评 | rational_critic |  | 是/否 | 待发 |

## 评论区任务

每条重点微博至少准备：

- 补充事实：
- 抛出分歧：
- 轻松接话：

## 今日复盘

表现最好的一条：

原因：

表现最差的一条：

原因：

明天继续追：

## 异常记录

审核提示：

限流迹象：

需要调整：
